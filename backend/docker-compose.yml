services:
  backend:
    build: .
    container_name: collabhub-backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped
